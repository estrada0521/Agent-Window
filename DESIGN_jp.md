## Agent Window の設計思想

[English](DESIGN.md)

Agent Windowは、複数のAIエージェントを管理するためのフレームワークではありません。既存のCLIを起動し、文字列を送り、その発話を一つの画面と一つの時系列で扱うための、意図的に薄くしたインターフェースです。

基本方針は、**自明なもの以外はなるべく何もしない**ことです。

一つのworkspace に紐ついた tmux sessionを一つの作業空間として扱い、そこに任意の複数のCLIを起動しても、会話をエージェントごとに分断しません。プロジェクトは一つであり、エージェントはその作業に一時的に参加します。CLI の終了、再起動を繰り返しても、記録は同じ時系列に残ります。

人間とAI、あるいはAI同士が文章を交わし、必要に応じて仕事を分けることを、Orchestration として扱う必要はありません。それは基本的には通常のコミュニケーションに過ぎず、role、handoff、task graph、共有メモリ、worktreeの所有権といった概念をアプリケーション側で先に定義することは、仕事がその形式に従う必要性という負債を生みます。自然言語が十分に表現できることを、別の制度へ変換する理由はありません。

エージェント間の通信も同じです。`agent-send`は、Agent が指定した別 Agent CLIへ文字列を入力するだけの最低レベルの機構であり、技術的には`tmux send-keys`のwrapperに過ぎません。受信側から見れば、人間が入力した文章と、別のエージェントから送られた文章に本質的な違いはありません。専用の通信プロトコルや共有mailboxを追加せず、人間が別のCLIへ文章を入力する操作を最小限の形で短絡しています。

統一ログは、人間が Agent に送ったメッセージ、Agent が人間に返したメッセージ、Agent が別の Agent に送ったメッセージを記録します。CLI の native log を監視して統合しているだけで、それ以上でもそれ以下でもありません。一方で、各CLIのnative logに含まれるtool callやcommand outputを無差別に記録するものではなく、人間と Agent が読むプロジェクト史・会話記録として成立する通常メッセージだけを共通の時系列へまとめます。

Agent Windowが担うのは、CLIの起動、入力の配送、発話の取得、ログの永続化、workspaceの状態反映といった、避けることのできない低層機構です。どのエージェントを使うか、どう仕事を分けるか、worktreeを使うか、誰が判断するかといった方針は、人間またはエージェントに残し、その知能を邪魔しません。

**不可避な機構は実装するが、知的主体が判断できることは制度化しない。** という事です。

モデルの能力不足を補うために追加された契約は、モデルが賢くなるほど負債になり得ます。Agent Windowは、エージェントの計画、分業、Git操作、context管理を自分の機能として再実装しません。上流のCLIとモデルが改善されれば、その能力をそのまま利用します。モデルの進歩を妨げず、過去の前提を固定しないための設計です。

対応するエージェントは、原則として、自ら大規模な計算資源を支配し、モデルを開発または直接運用し、それを一次的なCLIとして提供する長期的に存続可能な主体を優先します。第三者のAPIを共通インターフェースへ載せるだけのaggregatorや、provider-neutralなagent framework は、長期的に優位な主体として扱いません。
