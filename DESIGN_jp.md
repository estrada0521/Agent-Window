## Agent Window の設計思想

[English](DESIGN.md)

Agent Windowは、複数のAIエージェントを管理するためのフレームワークではありません。既存のCLIを起動し、文字列を送り、その発話を一つの画面と一つの時系列で扱うための、意図的に薄くしたインターフェースです。

機能の採否は一つの基準で決めます。**十分に賢い参加者が、考えれば済むことかどうか。** 済むことなら実装せず、済まないことなら実装します。前者は知能が向上するほど不要になり、後者はむしろ必要になります。結果として、自明なもの以外はなるべく何もしません。

一つのworkspace に紐ついた tmux sessionを一つの作業空間として扱い、そこに任意の複数のCLIを起動しても、会話をエージェントごとに分断しません。プロジェクトは一つであり、エージェントはその作業に一時的に参加します。CLI の終了、再起動を繰り返しても、記録は同じ時系列に残ります。

人間とAI、あるいはAI同士が文章を交わし、必要に応じて仕事を分けることを、Orchestration として扱う必要はありません。それは基本的には通常のコミュニケーションに過ぎず、role、handoff、task graph、共有メモリ、worktreeの所有権といった概念をアプリケーション側で先に定義することは、仕事がその形式に従う必要性という負債を生みます。自然言語が十分に表現できることを、別の制度へ変換する理由はありません。

調停は、作る機能ではなく、参加者がやることです。通路と記録があれば、レビュー、分担、上限の取り決めは、必要になった時点で必要な形で現れます。しかもそれらは理由を伴って現れます。配布された規約には、規則しか伴いません。

エージェント間の通信も同じです。`agent-send`は、Agent が指定した別 Agent CLIへ文字列を入力するだけの最低レベルの機構であり、技術的にはほとんど`tmux send-keys`のwrapperに過ぎません。受信側から見れば、人間が入力した文章と、別のエージェントから送られた文章に本質的な違いはありません。専用の通信プロトコルや共有mailboxを追加せず、人間が別のCLIへ文章を入力する操作を最小限の形で短絡しています。

統一ログは、人間が Agent に送ったメッセージ、Agent が人間に返したメッセージ、Agent が別の Agent に送ったメッセージを、起きた順に記録します。各CLIのnative logに含まれるtool callやcommand outputは記録しません。人間と Agent が読むプロジェクト史・会話記録として成立する通常メッセージだけを、共通の時系列へまとめます。

これはUIの機能ではなく、workspace内の一つのファイルです。Agent Windowはこれを所有せず、Agent Windowなしでも読めます。誰かが誤り、後に訂正した経緯も、要約されずに残ります。

Agent Windowが担うのは、CLIの起動、入力の配送、発話の取得、ログの永続化、workspaceの状態反映といった、知能ではなく機構でしか満たせない層です。どのエージェントを使うか、どう仕事を分けるか、worktreeを使うか、誰が判断するかといった方針は、人間またはエージェントに残し、その知能を邪魔しません。

**不可避な機構は実装するが、知的主体が判断できることは制度化しない。** という事です。

モデルの能力不足を補うために追加された契約は、モデルが賢くなるほど負債になり得ます。Agent Windowは、エージェントの計画、分業、Git操作、context管理を自分の機能として再実装しません。上流のCLIとモデルが改善されれば、その能力をそのまま利用します。モデルの進歩を妨げず、過去の前提を固定しないための設計です。

対応するエージェントは、自ら大規模な計算資源を支配し、モデルを開発または直接運用し、それを一次的なCLIとして提供する主体を優先します（Cursorは境界線上でしたが、この区分に入りつつあります）。Agent Windowはエージェントの限界を補う実装を持たないため、上流CLIの出来にそのまま依存します。接続先を選ぶのは、その帰結です。
