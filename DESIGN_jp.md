# 設計哲学

[English](DESIGN.md)

Agent Window は、**空間**（作業場所）、**時間**（履歴）、そしてその中で振る舞う**知能**を、モデル化せずに投影するための薄いインターフェースです。

## 前提

作業場所は既にあります。git repository、データベース、一時的な調査用ディレクトリ、あるいは論文を書くためだけのフォルダ。Agent Window はそれを workspace に紐づいた tmux session として一つの実装単位に対応させますが、その実態は問いません。

時間も既にあります。その痕跡は観測可能な変化の順序として、commit、ファイルの更新、プロセスの起動と終了に残っています。

そしてagentと人間は、その時空間に一時的あるいは半永続的に存在する知能です。どちらも同じ場所に居合わせ、同じ履歴を読み、互いに話しかけます。
ただし在り方は違います。agent は workspace の内側におり、ファイルを直接読みます。人間は外にいて、窓が要る。**Agent Window のGUIは、人間が agent を見るための薄い窓です。** 
窓が壊れても空間は壊れません。session を保持するのは tmux であり、GUI は外から覗くだけです。

## 統一ログ

人間 → agent、agent → 人間、agent → agent のメッセージや、Add / Remove Agent といった session topology を、Agent Window が時系列順に記録します。

これは記録の原本ではありません。原本は各 CLI の native log であり、git であり、ファイルシステムです。統一ログはそれらを、人間と agent が横断的に参照できる粒度へ落とした**射影**です。異なる path にある native log と、プロセスの再起動を跨いだ履歴が、ここで一つの時空間に合流します。

一部のentry — Add / Remove Agent のような session topology — は、どの native log からの射影でもなく、Agent Window 自身が行った操作の記録です。ただしこれは薄い historical な事実にとどまり、空間の第二モデルではありません。

各行のtimestampは Agent Window がそれを観測した時刻であり、由来する native log 内の event time ではありません。再起動後のcatch-upなどでは、実際の順序と観測順序が食い違うことがあります。

Agent Window は現実を所有しませんが、自分自身の射影が健全かどうかは知っていなければなりません。native log中の壊れた行はCLI側の記録であり、こちら側の失敗ではないので、スキップしてscanを続けます。ただしそのスキップは決して静かではなく、各agentは自分の窓が今晴れているかを示すprojection status(ok / warning + detail / unavailable + detail)を持ちます。

実体は  append-only jsonl であり、workspace に symlink されます。Agent Window に依存しません。ログはアプリケーション内部の状態ではなく、作業空間に残る記録です。

普段のやり取りは `agent-send` で直接行われます。他の参加者が何を担当し、どこで詰まっているかを横断的に把握したいときは、統一ログを読みます。厳密な再現が必要になったときは、各行が参照している native log と、その中での位置へ辿れます。

## 何を実装しないか

plan、role、task graph、handoff。これらは**十分に賢い知能が考えれば済むこと**です。

現在の LLM の能力不足を補うために設けた追加契約は、モデルの進化とともに陳腐化します。自然言語で表現でき、かつ既に現実の側に存在しているものを、固有の制度へ変換する理由はありません。

分離も同様です。保証は必要ですが、それは git primitive として、既に空間の中に存在しています。必要なら知能が git worktree を直接使えばよく、application が所有する必要はありません。所有すれば、すぐに次の問題が生じます。

- agent と worktree は 1:1 ではない。
- 両者の寿命は一致しない。
- application 外の git との2重SoTがになる。

Agent Window は、境界が明確で、作業を独立したタスクへ綺麗に分割できるプロジェクトだけを対象にはしません。**作業の分け方や進め方そのものが非自明**な仕事も扱うことを想定しています。

agent 間の通信も同様です。`agent-send` は技術的には `tmux send-keys` の極薄の wrapper であり、人間が CLI へ文章を入力する操作を最小限の形で短絡しているだけです。専用プロトコルも共有 mailbox も持ちません。

## 何を実装するか

判定基準は一つです。

> **参加者が現実の中から呼び出せないもの。**

これだけが不可避です。

- CLI の起動と、プロセスの生存
- 入力の投入と送信元prefixの付与
- native log の監視と、統一ログへの合流
- 作業空間の可観測化

CLI の起動は、呼ぶべき参加者がまだ存在しない時点で必要です。native log は agent ごとに別の path にあり、プロセスの再起動でも切れます。散った空間を一つへ合流させるには、全参加者の外側の視点が要ります。いずれも中からは届きません。

## 要約

不可避な機構は実装し、知的主体が判断できることは制度化しない。

新しい真実を重ねず、既にある現実を投影する。