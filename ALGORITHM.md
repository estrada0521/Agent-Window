# The Algorithm — 具体例

[AGENTS.md](AGENTS.md) の The Algorithm を、実際に起きた取り違えで補うための記録。

順序は常に同じ。

1. 要件を問う。そもそも何が満たされていればよいのか。
2. 削除する。問題そのものが存在しない形にできないか。
3. 単純化する。
4. 高速化する。
5. 自動化する。

以下の例はどれも、後ろの段から手をつけてしまったものです。

---

## 壊れた `.meta` の巻き添えを防ぐ

- 状況：あるセッションの `.meta` が壊れていると、Hub のセッション一覧がまるごと落ちる。
- 最初に出た手：壊れたセッションの行だけを「壊れている」と表示し、他のセッションは巻き込まないようにする。
- 本来の手：`.meta` を作るのも書くのも AW だけで、書き込みは原子的、形も固定されている。壊れた `.meta` は、人が意図的に壊したときにしか存在しない。要件は「壊れない」ことであり、読み手が壊れた状態を扱うコードごと削除する。
- 止めるべきだった段：1。巻き添えを防ぐ工夫は、要件と削除を飛ばした最適化だった。

## 二重 claim を下流で検出する

- 状況：同じ workspace を2つのセッションが claim していたら、読み取り時に raise していた。
- 最初に出た手：検出は残し、読み手で扱う。
- 本来の手：claim を書く入口（new session、workspace 変更）で弾くことが要件。入口が正しければ二重 claim は存在しない。下流の検出を消す。
- 止めるべきだった段：2。

## 同時 new session に備えてロックを入れる

- 状況：入口が「確認してから書く」形なので、同じ workspace への new session が同時に2つ来ると両方通る。
- 最初に出た手：Hub にロックを置き、一時ディレクトリを rename して原子的に出現させる。
- 本来の手：同時に同じ workspace で new session をすることは普通ない。起きるとすれば二重発火のバグで、直すべきはそのバグの側。実際に壊れるまで救済は入れない。
- 止めるべきだった段：1。起きうることと起きることは違う。

## `.meta` のないセッションフォルダを許容する

- 状況：create の途中で失敗すると、ログだけあって `.meta` のないフォルダが残る。読み手はそれを「workspace のないアーカイブ」として並べていた。
- 最初に出た手：読み手の許容を残す。
- 本来の手：フォルダ・ログ・`.meta` を create の最初にまとめて作る。その状態が生まれないので、読み手の許容を消せる。
- 止めるべきだった段：2。

## GUI の宛先に曖昧エラーを足す

- 状況：`claude-1` と `claude-2` がいるときに `claude` と送ると、GUI の経路は全員に送り、agent-send はエラーにしていた。
- 最初に出た手：GUI の経路も曖昧エラーに揃える。
- 本来の手：GUI が送る宛先は、画面のチップが持つ実名だけ。基本名はそもそも届かない。曖昧が生じるのは agent-send だけで、そこは送信時に弾いている。GUI 側の推測（基本名の展開、存在しない名前の素通し、`user` / `others` の特別扱い）をファイルごと消す。
- 止めるべきだった段：1。どの経路の話なのかを先に確かめる。

## ホスト名から「公開画面か」を推測する

- 状況：デスクトップの chat で、ファイルを開くときにホスト名から「別端末か」を推測していた。LAN 時代の IP 範囲が残っていた。
- 最初に出た手：推測をやめて、loopback かどうかの事実で判定する。
- 本来の手：デスクトップではファイルは必ず外部ビューアで開く。選択の余地がないので、判定そのものが要らない。
- 止めるべきだった段：2。推測を正す前に、分岐が要るのかを問う。

## session-state の部分更新プロトコル

- 状況：変更イベントに seq、128件の履歴、resync、部分 projection を持たせ、どの断片が変わったかを復元していた。
- 本来の手：中身はエージェント名、running/idle、thinking の一行程度。イベントが来たら全部取り直せば足りる。
- 止めるべきだった段：1。最適化の前に、その最適化が守っているものがあるかを問う。

## 統合ログの壊れた行を読み飛ばす

- 状況：統合ログの読み手が、壊れた行・空行・object でない行を分類して読み飛ばしていた。
- 本来の手：書き手は AW だけで、1行を1回の write で書く。存在しうるのは「書き込み途中の末尾行」だけで、それは壊れているのではなく、ふつうの並行状態。それ以外の分類を消し、`json.loads` に任せる。
- 付記：分類を消したら、末尾行が 64KB を超えて前のブロックにまたがるとその先頭が1行として読まれる、という潜在的な欠陥が表に出た。許容が第一の経路の欠陥を隠していた例。救済を戻すのではなく、末尾行の除去そのものを直した。

## chat server が止まっていた間の出力を拾い直す

- 状況：chat server の再起動後はネイティブログを末尾から読むので、止まっていた間の agent の出力は統合ログに入らない。
- 最初に出た手：止まっていた間をさかのぼって投影する。
- 本来の手：AW を終了しても chat server は死なない。死んでいたとしたら、それ自体が別の意味で壊れている。「死んでいたら」という仮定が誤りで、過去にさかのぼってはいけない。
- 止めるべきだった段：1。

## 送信文を strip する

- 状況：送信経路の3箇所（フロントとサーバ2箇所）が、本文の前後の空白を削っていた。コードの字下げや末尾の改行が消える。
- 本来の手：誰も意図していない変形だった。空白だけかどうかの判定にだけ strip を使い、送る中身には触れない。
- 教訓：「念のための正規化」も、入力を書き換える以上は要件の側で説明できなければならない。

---

## 許容してよいもの（対比）

同じ「失敗を扱うコード」でも、次のものは残す。違いは、その状態が AW の外の現実にあるかどうか。

- ネイティブログ（Claude や Codex が書くファイル）が消える・rename される。外の世界ではふつうに起きること。ただし1人の agent の失敗で、他の agent の監視を止めてはいけない。
- `ps` が読めないときに、tmux が知っている pane の pid だけで調べる。外部依存の幅。
- ネイティブログが現れるまで、送信直後に数秒ポーリングする。外部依存の幅として意図的。
- 追記中のファイルの、未完結の末尾行。並行して読み書きする以上ふつうに起きる。

## テストについて

存在しえない状態を固定しているテストは、その経路と一緒に消す。例：workspace のない `.meta`、空の workspace を渡したときの ValueError、自分が直前に書いた値の検証。
