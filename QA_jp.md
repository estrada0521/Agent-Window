# Q&A

[English](QA.md)

### 一般向けの製品ですか？

いいえ。作者が実際に使用している個人用ツールを、そのまま公開しています。
利用、改造、流用は自由ですが、導入支援や動作保証はありません。

### なぜmacOSだけなのですか？

作者がmacOSで使用しているためです。
クロスプラットフォーム対応は目的としていません。

### native logの形式変更にはどう対応しますか？

作者が問題に遭遇した時点で、形式変更に合わせて必要な箇所を直します。
モデルの更新を跨いで継続利用していますが、現時点で問題はありません。

### なぜSDKや構造化プロトコルを使わないのですか？

各CLIの認証、サブスクリプション、コマンド、コンテキスト管理を、そのまま利用するためです。
実行主体は通常のCLIなので、Agent Windowが停止してもターミナルから操作できます。

### なぜtmuxを使うのですか？

最低レベルの確立されたものだからです。
乱立する〇〇mux系のツールは、すでに多機能すぎます。

### なぜ送信に `tmux send-keys` を使うのですか？

必要なのが文字列の入力だけだからです。
人間からの入力と、Agentから別のAgentへの入力を区別していません。
失敗ケースはほぼ皆無であり、acknowledgement、retry、idempotencyの制度化は過剰です。

### なぜAgent間通信にMCPを使わないのですか？

文章を送るだけなら、相手のCLIへ入力すれば済むためです。
専用プロトコルを追加しても、実装と保守の対象が増えるだけです。

### なぜエージェントごとにチャットを分けないのですか？

会話の単位がプロジェクトだからです。
分ければ、同じ作業の記録が参加者の数だけ分断されます。

### なぜtool callを共通JSONLへ保存しないのですか？

共通JSONLを、意味のある会話記録として保つためです。
tool callは画面上では表示しますが、保存すると大部分がノイズになります。必要ならnative logを参照します。

### 統一ログは何に使えますか？

人間とAgentが読む、プロジェクトの会話記録です。
workspace内から参照できるため、Agentに状況を把握させる用途にも使えます。形式は追記のみのJSONLで、Agent Windowがなくても読めます。

### 会話履歴や添付ファイルはどこに保存されますか？

会話履歴は `~/.agent-window/session/{session_name}/.log.jsonl` に保存されます。workspace内の `.agent-window/.log.jsonl` にはsymlinkが張られるため、エージェントからも簡単に参照できます。
添付ファイルはworkspace内の `.agent-window/uploads/` に保存されます。

### Agent Windowから外部へデータを送信しますか？

いいえ。Agent Window自体は外部へ一切のデータを送信しません。
各Agent CLIが行う通信は、それぞれのCLI自身のものです。

### なぜtask graphやrole管理を実装しないのですか？

自然言語と既存ツールで扱えることを、Agent Window独自の制度として固定しないためです。

### なぜworktreeを自動管理しないのですか？

必要性や分割方法を一律に決められないためです。
必要なら、人間またはAgentが通常のGit操作として扱います。

### 複数のAgentが同じworkspaceを編集して競合しませんか？

Agent次第です。互いに気をつけていれば競合しません。
Agent Window側では固定せず、Agentの知能に任せます。

### 並列数や計算資源の上限は管理しますか？

しません。適切な上限は状況によって変わります。
必要な取り決めは、人間またはAgentがその場で行います。

### なぜ `0.0.0.0` にbindするのですか？

LAN上のiPhoneからPWAを使用するためです。
信頼できるローカルネットワークでの利用を前提としており、認証は実装していません。作者の環境では、PWAと認証の両立は無用な保守対象を増やすだけです。
必要な場合は、ご自身でbind先の変更や認証の追加をお願いします。
